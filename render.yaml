services:
  - type: web
    name: google-mcp-server
    runtime: docker
    plan: starter  # Use 'free' for testing, 'starter' for production ($7/mo)
    envVars:
      - key: GOOGLE_CREDENTIALS_B64
        sync: false  # You will set this manually in Render dashboard
